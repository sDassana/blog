Place your brand logo image here as "brand.png".

Recommended:
- PNG with transparent background
- Size around 240x60 (it will be scaled down via CSS to ~24–32px height)
- File path used in code: /blog/public/assets/brand.png

If you prefer a different filename, update the src attribute in public/partials/topbar.php accordingly.